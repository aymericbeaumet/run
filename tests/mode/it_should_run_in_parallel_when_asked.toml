mode = "parallel"

[[run]]
name = "finish second"
cmd = ["coreutils", "timeout", "--foreground", "--verbose", "0.2s", "coreutils", "cat"]

[[run]]
name = "finish first"
cmd = ["coreutils", "timeout", "--foreground", "--verbose", "0.1s", "coreutils", "cat"]
