mode = "sequential"
workdir = "$CARGO_MANIFEST_DIR/tests/--print-config"

[run.echo]
cmd = [
    "echo",
    "ok",
]
tags = []

[tmux]
kill_duplicate_session = true
program = "tmux"
session_prefix = "run-"
socket_path = "/tmp/tmux.run.sock"

